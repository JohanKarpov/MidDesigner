window.LAYOUT_OVERRIDES = {
    enabled: false,
    // Optional global CSS variables (example)
    cssVariables: {
        // '--container-width': 'min(90vw, 900px, calc(95vh * 9 / 16))'
    },
    // Selector -> inline style map (camelCase keys)
    elements: {
        // '#generate-btn': {
        //     left: '50%',
        //     bottom: '8%',
        //     width: '54%',
        //     transform: 'translateX(-50%)'
        // }
    }
};
